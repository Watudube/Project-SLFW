/**
 * Scene Keys
 *
 * Contains the unique keys for all Phaser game scenes.
 * Using Object.freeze() to prevent accidental modifications.
 */
export const SCENE_KEYS = Object.freeze({
  OVERWORLD_SCENE: "OVERWORLD_SCENE",
});
